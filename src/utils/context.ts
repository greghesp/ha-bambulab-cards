import { createContext } from "@lit/context";

export const hassContext = createContext("hass");
export const deviceEntitesContext = createContext("device_entities");
export const infoBarContext = createContext("info_bar");
